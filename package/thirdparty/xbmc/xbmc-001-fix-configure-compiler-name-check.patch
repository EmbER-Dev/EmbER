configure: buildroot crosscompiler name is a bit different.

signed-off-by Rene van Dorst <opensource@vdorst.com>

--- a/configure.in
+++ b/configure.in
@@ -16792,7 +16792,7 @@ case $host in
      ARCH_DEFINES="-DTARGET_POSIX -DTARGET_LINUX -D_LINUX -D_POWERPC64"
 
      ;;
-  arm*-*-linux-gnu*)
+  arm*-*-linux-*gnu*)
      use_texturepacker_native=yes
      ARCH="arm"
      use_arch="arm"
